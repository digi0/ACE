# ---------- Public URL ----------
# If you have a domain, use it, for example: ace.yourdomain.com
DOMAIN=localhost
PUBLIC_BASE_URL=http://localhost

# ---------- Private access gate ----------
# Choose your own username.
BASIC_AUTH_USER=owner
# Generate hash:
# docker run --rm caddy:2.8 caddy hash-password --plaintext "your-strong-password"
BASIC_AUTH_HASH=$$2a$$14$$REPLACE_ME

# ---------- Backend secrets/config ----------
MONGO_URL=mongodb://mongo:27017
DB_NAME=ace_private
EMERGENT_LLM_KEY=REPLACE_WITH_YOUR_KEY
CORS_ORIGINS=http://localhost

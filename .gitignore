node_modules/
.env
.env.local
.env.*.local
.env.development
.env.production
.DS_Store
__pycache__/
*.pyc
.venv/
# backend/data/ace_index.pkl  # committed so Railway doesn't need to rebuild
# Local SQLite dev DB (user data) + its WAL/SHM/journal sidecars — prod uses Postgres on Railway. Never commit.
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal
backend/data/*.db-journal
backend/uploads/
psu-cs-academic-advisor-main/
deploy/
frontend/nginx.conf
